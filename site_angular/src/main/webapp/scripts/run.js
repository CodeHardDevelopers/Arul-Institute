'use strict';

angular.module('ArulInstitute')
    .run(run);

/**
 * @ngdoc function
 * @name run
 * @description
 * Fires up the app
 */


function run($rootScope,$state) {
    console.log('App Run');
}
